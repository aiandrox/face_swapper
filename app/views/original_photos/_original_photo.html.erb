<div id="<%= dom_id original_photo %>">
  <% if original_photo.processed_photo.attached? %>
    <div style="max-width: 100%">
      <%= image_tag original_photo.processed_photo, width: "100%" %>
    </div>
  <% end %>
</div>
