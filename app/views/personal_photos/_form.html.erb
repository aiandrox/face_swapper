<%= form_with(model: personal_photo) do |form| %>
  <% if personal_photo.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(personal_photo.errors.count, "error") %> prohibited this personal_photo from being saved:</h2>

      <ul>
        <% personal_photo.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :photo, style: "display: block" %>
    <%= form.file_field :photo %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
