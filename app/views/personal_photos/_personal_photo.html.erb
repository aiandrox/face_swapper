<div id="<%= dom_id personal_photo %>">
  <p>
    <strong>Name:</strong>
    <%= personal_photo.name %>
  </p>

  <% if personal_photo.photo.attached? %>
    <p>
      <strong>Photo:</strong>
      <%= image_tag personal_photo.photo, width: "100%" %>
    </p>
  <% end %>

  <% if personal_photo.icon.attached? %>
    <p>
      <strong>Icon:</strong>
      <%= image_tag personal_photo.icon, width: "100%" %>
    </p>
  <% end %>
</div>
